/** Auto sermon clips paused — only Go Deeper uses curated teaching clips for now. */
interface ResourceSuggestionCardProps {
  messages: { role: string; content: string }[];
  topic?: string;
}

export function ResourceSuggestionCard(_props: ResourceSuggestionCardProps) {
  return null;
}
