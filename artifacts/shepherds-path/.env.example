# api-server base URL (must match PORT in artifacts/api-server/.env)
VITE_API_PROXY_TARGET=http://localhost:8080

# Optional: call api-server directly (skips Vite proxy). Example:
# VITE_API_BASE_URL=http://localhost:8080

# Vite dev server port (keep different from api-server)
PORT=3000
