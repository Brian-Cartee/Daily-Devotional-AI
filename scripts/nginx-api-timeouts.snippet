# Add inside the server { } block that proxies /api to the Node API (Lightsail).
# Journey generation can take 30–90s; default proxy timeouts are often 60s.
#
#   location /api/ {
#     proxy_pass http://127.0.0.1:3001;
#     proxy_read_timeout 120s;
#     proxy_connect_timeout 30s;
#     proxy_send_timeout 120s;
#   }
